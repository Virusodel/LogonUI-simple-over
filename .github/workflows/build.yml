name: Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.1
    
    - name: Restore NuGet packages
      run: |
        nuget restore CustomLogonUI/CustomLogonUI.csproj
      shell: cmd
    
    - name: Build CustomLogonUI
      run: |
        msbuild CustomLogonUI/CustomLogonUI.csproj /p:Configuration=Release /p:Platform=AnyCPU
      shell: cmd
    
    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: CustomLogonUI
        path: CustomLogonUI\bin\Release\net48\CustomLogonUI.exe
