name: Build

on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
    - name: Build CustomLogonUI
      run: |
        msbuild CustomLogonUI.csproj /p:Configuration=Release /p:Platform="Any CPU"
        copy CustomLogonUI\bin\Release\CustomLogonUI.exe LogonUI.exe
    - name: Build Installer
      run: msbuild LogonUIInstaller.csproj /p:Configuration=Release /p:Platform="Any CPU"
    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: payload
        path: LogonUIInstaller\bin\Release\LogonUIInstaller.exe
